import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'conceptos_basicos_angular',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
